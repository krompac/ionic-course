import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.course',
  appName: 'ionic course',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
